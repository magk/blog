<h1>Edit Post</h1>
<%= form_for :post, url: '/posts/' + @post.id.to_s, method: :patch do |f| %>

<p>
	<%= f.label :title %>
	<%= f.text_field :title %>
</p>

<p>
	<%= f.label :content %>
	<%= f.text_area :content %>
</p>

<p>
	<%= f.submit %>
</p>
<% end %>
	